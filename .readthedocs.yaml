version: 2

build:
  os: ubuntu-22.04
  tools:
    python: '3.11'
  jobs:
    pre_build:
      - python --version

mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - requirements: docs/requirements.txt
